# Tested with: Python 3.12, numpy 2.5.0, Pillow 12.2.0, scikit-learn 1.9.0, joblib 1.5.3
# A fresh `pip install -r requirements.txt` installs versions that load model.joblib.
# model.joblib is a scikit-learn pickle, so use a recent scikit-learn to load it.
numpy>=1.24
Pillow>=10.0
scikit-learn>=1.6
joblib>=1.3
